export class FocusLogList {
    constructor(postId, focusLogId, title, rating, postCreatedAt, totalFocusedTime) {
        this.postId = postId;
        this.focusLogId = focusLogId;
        this.title = title;
        this.rating = rating;
        this.postCreatedAt = postCreatedAt;
        this.totalFocusedTime = totalFocusedTime;
    }
}

